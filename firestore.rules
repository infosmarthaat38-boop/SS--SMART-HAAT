
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {

    // FORCE REDEPLOY IDENTIFIER: 20240522_CRITICAL_PERMISSIVE_V9
    // This comment ensures the rules are re-evaluated and applied immediately across all nodes.

    match /{path=**} {
      allow read, write: if true;
    }
  }
}
